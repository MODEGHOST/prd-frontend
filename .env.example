# Leave empty when frontend and backend share the same origin (recommended).
# Empty = use Vite proxy / reverse proxy so httpOnly cookies work.
# Set an explicit HTTP(S) origin only when Socket.IO is hosted separately
# (then also configure COOKIE_SAME_SITE=none on the backend).
VITE_SOCKET_URL=
